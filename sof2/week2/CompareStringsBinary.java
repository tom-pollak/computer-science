import java.util.Scanner;

public class CompareStringsBinary {
    public static void main(String[] args) {
        Scanner keyboard = new Scanner(System.in);
        System.out.print("Enter a number");
        String inputA = keyboard.nextLine();
        System.out.print("Enter a number");
        String inputB = keyboard.nextLine();
        String output = "";
        if (Math.max(inputA.length(), inputB.length()))
        Integer minLength = Math.min(inputA.length(), inputB.length());
        for (int i = 0; i < maxLength; i++) {
            if (minLength < i) {
                output = output + Math.max(inputA.length(), b)
            }
        }
    }
}
